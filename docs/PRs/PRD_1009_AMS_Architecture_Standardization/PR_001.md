status: closed

# PR-001: File De-duplication and Import Refactoring

## 1. Objective
Clean up duplicate files and establish a single source of truth for `qmt_client.py` within the AMS project, and fix test imports.

## 2. Scope & Implementation Details
- **Delete `AMS/qmt_client.py`**: Remove the redundant legacy version at the root of the `AMS/` directory.
- **Retain `AMS/scripts/qmt_client.py`**: Ensure this is the single source of truth for the QMT Client implementation.
- **Refactor `AMS/tests/test_qmt_client.py`**: Update the import statements to correctly resolve `scripts.qmt_client.QMTClient`. This may involve adding `__init__.py` files or dynamically adjusting `sys.path` in the test file to ensure imports work cleanly without `ImportError`.
- **Refactor `pilot_stock_radar.py`**: If it exists and imports `qmt_client`, update its imports to match the new structure.

## 3. TDD & Acceptance Criteria
- `ls AMS/qmt_client.py` must return an error (file should not exist).
- `AMS/scripts/qmt_client.py` must exist.
- `pytest AMS/tests/test_qmt_client.py` runs and passes successfully with 100% green tests.
- No `ImportError` or `ModuleNotFoundError` during test execution.